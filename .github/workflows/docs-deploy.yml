name: Docs — Deploy to GitHub Pages

# One-time setup (manual, after first run):
#   Settings → Pages → Source: "Deploy from a branch" → gh-pages / (root)
#   Settings → Actions → General → Workflow permissions: "Read and write"
# The gh-pages branch is auto-created on first successful deploy.

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'vocs.config.ts'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/docs-deploy.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: docs-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and publish to gh-pages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.8.0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright chromium (for Vocs mermaid rendering)
        run: pnpm dlx playwright install --with-deps chromium

      - name: Build docs
        run: pnpm run docs:build
        env:
          BASE_PATH: /claude-second-brain

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/dist
          publish_branch: gh-pages
          keep_files: true
          force_orphan: false
